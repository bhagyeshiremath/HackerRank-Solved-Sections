/* Firstly import the library for BigInteger 
* ie : import java.util.math.BigInteger
* Otherwise it will show an error cannot find symbol BigInteger
*/
public static void main(String[] args) {
        Scanner scn = new Scanner (System.in);
        BigInteger a = new BigInteger(scn.next());
        BigInteger b = new BigInteger(scn.next());
        System.out.println(a.add(b));
        System.out.println(a.multiply(b));       
}
